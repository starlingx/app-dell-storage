From b961fab6441d0b7c92778b188005171b71ec2121 Mon Sep 17 00:00:00 2001
From: Guilherme Costa <guilherme.costa@windriver.com>
Date: Fri, 1 Mar 2024 12:54:34 -0300
Subject: [PATCH] csi-unity: add storageclass in templates

This patch consists of adding a storage classes in the helm chart
templates directory.
It's needed because this helm chart needs a storage classes that should
be created manually.

Signed-off-by: Guilherme Costa <guilherme.costa@windriver.com>
Signed-off-by: Gustavo Ornaghi Antunes <gustavo.ornaghiantunes@windriver.com>
---
 charts/csi-unity/templates/storageclass.yaml | 50 ++++++++++++++++++++
 charts/csi-unity/values.yaml                 |  2 +
 2 files changed, 52 insertions(+)
 create mode 100644 charts/csi-unity/templates/storageclass.yaml

diff --git a/charts/csi-unity/templates/storageclass.yaml b/charts/csi-unity/templates/storageclass.yaml
new file mode 100644
index 0000000..52ee79c
--- /dev/null
+++ b/charts/csi-unity/templates/storageclass.yaml
@@ -0,0 +1,50 @@
+{{- range $sc := .Values.storageClasses }}
+---
+apiVersion: storage.k8s.io/v1
+kind: StorageClass
+metadata:
+{{- if or $sc.name (and $sc.arrayId $sc.protocol)}}
+  name: {{ $sc.name | default ( print "unity-" $sc.arrayId "-" $sc.protocol | lower ) }}
+{{- else if $sc.arrayId }}
+  name: {{ print "unity-" $sc.arrayId "-nfs" | lower }}
+{{- end }}
+provisioner: {{ $sc.provisioner | default ( print "csi-unity.dellemc.com" ) }}
+reclaimPolicy: {{ $sc.reclaimPolicy | default "Delete" }}
+allowVolumeExpansion: {{ $sc.allowVolumeExpansion | default "true" }}
+volumeBindingMode: {{ $sc.volumeBindingMode | default "Immediate" }}
+{{- if $sc.allowedTopologies }}
+allowedTopologies:
+  {{ $sc.allowedTopologies | toYaml | nindent 4 }}
+{{- end }}
+{{- if eq $sc.protocol "NFS" }}
+mountOptions: {{ $sc.mountOptions | default "['hard']" }}
+{{- else if $sc.protocol }}
+mountOptions: {{ $sc.mountOptions | default "[]" }}
+{{- else }}
+mountOptions: {{ $sc.mountOptions | default "['hard']" }}
+{{- end }}
+parameters:
+  protocol: {{ $sc.protocol | default "NFS" }}
+  thinProvisioned: "{{- $sc.thinProvisioned | default true }}"
+  isDataReductionEnabled: "{{- $sc.isDataReductionEnabled | default true }}"
+  hostIoSize: "{{ $sc.hostIoSize | default 8192 }}"
+{{- if $sc.arrayId }}
+  arrayId: {{ $sc.arrayId }}
+{{- end }}
+{{- if $sc.storagePool }}
+  storagePool: {{ $sc.storagePool }}
+{{- end }}
+{{- if $sc.tieringPolicy }}
+  tieringPolicy: "{{ $sc.tieringPolicy }}"
+{{- end }}
+{{- if and ($sc.nasServer) (or (eq $sc.protocol "NFS") (not $sc.protocol)) }}
+  nasServer: {{ $sc.nasServer }}
+{{- end }}
+{{- if $sc.hostIOLimitName }}
+  hostIOLimitName: "{{ $sc.hostIOLimitName }}"
+{{- end }}
+{{- if $sc.fstype }}
+  csi.storage.k8s.io/fstype: {{ $sc.fstype | lower | default "nfs" }}
+{{- end }}
+---
+{{- end }}
diff --git a/charts/csi-unity/values.yaml b/charts/csi-unity/values.yaml
index 900f4d4..a22c480 100644
--- a/charts/csi-unity/values.yaml
+++ b/charts/csi-unity/values.yaml
@@ -266,3 +266,5 @@ storageCapacity:
   pollInterval: 5m
 
 secret:
+
+storageClasses:
-- 
2.34.1
