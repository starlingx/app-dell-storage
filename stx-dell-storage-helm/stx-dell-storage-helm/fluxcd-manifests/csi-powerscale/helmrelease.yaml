#
# Copyright (c) 2024 Wind River Systems, Inc.
#
# SPDX-License-Identifier: Apache-2.0
#

apiVersion: "helm.toolkit.fluxcd.io/v2"
kind: HelmRelease
metadata:
  name: csi-powerscale
  labels:
    chart_group: dell-storage-charts
spec:
  releaseName: csi-powerscale
  chart:
    spec:
      chart: csi-isilon
      version: REPLACE_HELM_CHART_VERSION
      sourceRef:
        kind: HelmRepository
        name: stx-platform
  interval: 1m
  timeout: 30m
  test:
    enable: false
  install:
    disableHooks: false
  upgrade:
    disableHooks: false
  valuesFrom:
    - kind: Secret
      name: csi-powerscale-static-overrides
      valuesKey: csi-powerscale-static-overrides.yaml
    - kind: Secret
      name: csi-powerscale-system-overrides
      valuesKey: csi-powerscale-system-overrides.yaml
